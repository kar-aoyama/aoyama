jar.enabled = true

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web")
    compile('org.springframework.cloud:spring-cloud-starter-openfeign')

    compile 'io.github.openfeign:feign-httpclient:10.9'
}